services:
  - type: web
    name: fum-proxy
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: ALLOWED_ORIGIN
        value: https://freeuploadmanager.org
